# حل مشكلة: Could not resolve host: api.telegram.org

## 🔴 المشكلة
الخادم لا يستطيع الاتصال بـ Telegram API بسبب عدم القدرة على حل اسم النطاق (DNS Resolution).

## ✅ الحلول المتاحة

### الحل 1: التحقق من اتصال الإنترنت (الأهم)

#### خطوة 1: اختبر الاتصال من CMD
افتح CMD كمسؤول وشغّل:
```bash
ping api.telegram.org
```

**إذا لم يعمل:**
```bash
ping 8.8.8.8
```

#### خطوة 2: اختبر DNS
```bash
nslookup api.telegram.org
```

**إذا فشل، جرب:**
```bash
nslookup api.telegram.org 8.8.8.8
```

---

### الحل 2: تغيير DNS في Windows

#### الطريقة A: استخدام Google DNS
1. افتح **لوحة التحكم** → **الشبكة والإنترنت** → **مركز الشبكة والمشاركة**
2. اضغط على اتصالك النشط
3. اضغط **خصائص** → **Internet Protocol Version 4 (TCP/IPv4)**
4. اختر **استخدام عناوين خوادم DNS التالية**
5. ضع:
   - **المفضل:** `8.8.8.8`
   - **البديل:** `8.8.4.4`
6. اضغط **موافق**

#### الطريقة B: استخدام Cloudflare DNS
- **المفضل:** `1.1.1.1`
- **البديل:** `1.0.0.1`

---

### الحل 3: إعدادات Proxy في PHP

إذا كنت خلف Proxy، أضف هذا الكود في `includes/simple_telegram.php`:

```php
// في دالة sendTelegramMessage (السطر 54)
curl_setopt_array($ch, [
    CURLOPT_URL => $url,
    CURLOPT_POST => true,
    CURLOPT_POSTFIELDS => http_build_query($data),
    CURLOPT_RETURNTRANSFER => true,
    CURLOPT_SSL_VERIFYPEER => false,
    CURLOPT_TIMEOUT => 10,
    CURLOPT_CONNECTTIMEOUT => 5,
    // إضافة هذه الأسطر إذا كنت تستخدم Proxy
    // CURLOPT_PROXY => 'proxy_address:port',
    // CURLOPT_PROXYUSERPWD => 'username:password',
]);
```

---

### الحل 4: تعطيل IPv6 مؤقتاً

في بعض الأحيان IPv6 يسبب مشاكل:

1. افتح **لوحة التحكم** → **الشبكة والإنترنت**
2. **مركز الشبكة والمشاركة** → **تغيير إعدادات المحول**
3. انقر بزر الفأرة الأيمن على اتصالك → **خصائص**
4. ألغِ تحديد **Internet Protocol Version 6 (TCP/IPv6)**
5. اضغط **موافق**

---

### الحل 5: تعديل ملف hosts (مؤقت للاختبار)

إذا كانت المشكلة في DNS فقط، يمكنك إضافة IP يدوياً:

1. افتح المفكرة **كمسؤول**
2. افتح ملف: `C:\Windows\System32\drivers\etc\hosts`
3. أضف هذا السطر في نهاية الملف:
```
149.154.167.220 api.telegram.org
```
4. احفظ الملف

---

### الحل 6: استخدام IP بدلاً من Domain (حل سريع)

عدّل ملف `includes/simple_telegram.php`:

```php
// استبدل السطر 21
// من:
define('TELEGRAM_API_URL', 'https://api.telegram.org/bot' . TELEGRAM_BOT_TOKEN);

// إلى:
define('TELEGRAM_API_URL', 'https://149.154.167.220/bot' . TELEGRAM_BOT_TOKEN);
```

⚠️ **تحذير:** قد يتغير IP، لذا هذا حل مؤقت فقط.

---

### الحل 7: إعادة تشغيل خدمات الشبكة

افتح CMD كمسؤول وشغّل:

```bash
ipconfig /flushdns
ipconfig /registerdns
ipconfig /release
ipconfig /renew
netsh winsock reset
```

ثم أعد تشغيل الكمبيوتر.

---

### الحل 8: تعطيل Firewall/Antivirus مؤقتاً

بعض برامج الحماية تمنع PHP من الاتصال بالإنترنت:

1. عطّل Windows Firewall مؤقتاً
2. عطّل Antivirus مؤقتاً
3. اختبر مرة أخرى
4. إذا نجح، أضف استثناء لـ `php.exe` و `httpd.exe` (Apache)

---

### الحل 9: السماح لـ PHP/Apache في Firewall

#### Windows Firewall:
1. **لوحة التحكم** → **جدار حماية Windows**
2. **السماح بتطبيق عبر جدار الحماية**
3. **السماح بتطبيق آخر**
4. أضف: `C:\xampp\php\php.exe`
5. أضف: `C:\xampp\apache\bin\httpd.exe`

---

### الحل 10: فحص php.ini

تأكد من أن cURL مفعّل:

1. افتح `C:\xampp\php\php.ini`
2. ابحث عن: `;extension=curl`
3. احذف `;` لتصبح: `extension=curl`
4. أعد تشغيل Apache

---

## 🧪 اختبار الحلول

بعد تطبيق أي حل، اختبر من CMD:

```bash
cd C:\xampp\htdocs\tr
php -r "echo file_get_contents('https://api.telegram.org/bot6286098014:AAGr6q-6mvUHYIa3elUkssoijFhY7OXBrew/getMe');"
```

---

## 📝 الحل الموصى به (بالترتيب)

1. ✅ **غيّر DNS إلى Google DNS** (8.8.8.8)
2. ✅ **امسح DNS Cache** (`ipconfig /flushdns`)
3. ✅ **أعد تشغيل Apache**
4. ✅ **اختبر مرة أخرى**

---

## 🔧 حل بديل: استخدام HTTP بدلاً من HTTPS (للاختبار فقط)

في `includes/simple_telegram.php`:

```php
// تغيير مؤقت للاختبار
define('TELEGRAM_API_URL', 'http://api.telegram.org/bot' . TELEGRAM_BOT_TOKEN);
```

⚠️ **غير آمن - للاختبار فقط!**

---

## 💡 إذا استمرت المشكلة

المشكلة قد تكون:
- 🚫 مزود الإنترنت يحجب Telegram
- 🚫 الشبكة (مثل شبكة العمل/المدرسة) تحجب Telegram
- 🚫 الدولة تحجب Telegram

**الحل:** استخدم VPN أو Proxy

